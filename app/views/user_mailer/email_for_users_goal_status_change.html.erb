<h1> Hello, <%= @user.name %> </h1>
  <div>
    <br>
      <% if @goal.status == "closed_succ" %> 
        <p> We want to inform you that your goal "<%= @goal.name %>" has been
        successfully closed! <p>
      <% else %>
        <p> We want to inform you that your goal's "<%= @goal.name %>" status 
          was changed from <%= @old_status.humanize %> to <%= @goal.status.humanize %> </p>
      <% end %>
      <br>
      <p> With regards, GTS team </p>
  </div>
